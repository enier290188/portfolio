const GROUP_ROOT = 'Root' as const
const GROUP_ADMIN = 'Admin' as const
const GROUP_SALE = 'Sale' as const
const GROUP_PROJECT = 'Project' as const

const USER_GROUP_LIST = [GROUP_ROOT, GROUP_ADMIN, GROUP_SALE, GROUP_PROJECT] as const

export const value = {
    GROUP_ROOT,
    GROUP_ADMIN,
    GROUP_SALE,
    GROUP_PROJECT,
    USER_GROUP_LIST,
}
