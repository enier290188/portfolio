import { useLocation, useMatch, useNavigate, useParams, useResolvedPath, useSearchParams } from 'react-router-dom'

export const hook = {
    useLocation,
    useMatch,
    useNavigate,
    useParams,
    useResolvedPath,
    useSearchParams,
}
