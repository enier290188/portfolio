import { fn } from './fn'
import { hook } from './hook'
import { render } from './render'
import * as tableType from './type'

export const table = {
    fn,
    hook,
    render,
}

export type { tableType }
