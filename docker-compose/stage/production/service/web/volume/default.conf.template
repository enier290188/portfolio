server {
    server_name ${NGINX_HOSTNAME};
    listen ${NGINX_PORT};
    client_max_body_size 4G;

    location / {
        root /usr/src/service/web;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
